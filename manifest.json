{
  "manifest_version": "0.3",
  "name": "thread-mcp",
  "display_name": "Thread MCP",
  "description": "thread-mcp is the Jupyter notebook for LLM, optimized for natural languages. Thread helps you share, reproduce, update, and resume LLM conversations across different LLM clients.",
  "version": "1.2.2",
  "author": {
    "name": "Yuri Gui",
    "url": "https://github.com/yugui923"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/yugui923/thread-mcp.git"
  },
  "homepage": "https://github.com/yugui923/thread-mcp#readme",
  "support": "https://github.com/yugui923/thread-mcp/issues",
  "license": "GPL-3.0",
  "icon": "icon.png",
  "server": {
    "type": "node",
    "entry_point": "dist/index.js",
    "mcp_config": {
      "command": "node",
      "args": ["${__dirname}/dist/index.js"],
      "env": {
        "THREAD_MCP_STORAGE_DIR": "${user_config.storage_dir}"
      }
    }
  },
  "user_config": {
    "storage_dir": {
      "type": "directory",
      "title": "Storage Directory",
      "description": "Directory for storing conversation threads",
      "default": "${HOME}/.thread-mcp",
      "required": false
    }
  },
  "privacy_policies": [],
  "compatibility": {
    "platforms": ["darwin", "win32", "linux"],
    "runtimes": {
      "node": ">=22"
    }
  },
  "tools": [
    {
      "name": "save_thread",
      "description": "Save a conversation thread to local storage or a remote server"
    },
    {
      "name": "find_threads",
      "description": "Find saved conversation threads by ID, title, query, or filters"
    },
    {
      "name": "update_thread",
      "description": "Update an existing conversation thread with new messages or metadata"
    },
    {
      "name": "delete_thread",
      "description": "Delete a saved conversation thread by ID or title"
    },
    {
      "name": "resume_thread",
      "description": "Load a saved conversation thread to continue where you left off"
    }
  ]
}
