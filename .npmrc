# Refuse to install npm packages published less than 7 days ago.
# Defense against compromised-maintainer publishes (typically yanked
# within hours). Requires npm >= 11.10.0; value is in DAYS (unlike
# pnpm/Yarn which use minutes, or Bun which uses seconds).
min-release-age=7
